docker buildx build \
  --platform linux/amd64 \
  -f Dockerfile.syslog \
  -t rbuffing/syslogdedup-syslog:1.16 \
  --push \
  .

docker buildx build \
  --platform linux/amd64 \
  -f Dockerfile.web \
  -t rbuffing/syslogdedup-web:1.15 \
  --push \
  .
